# Editor / OS noise
.DS_Store
*.swp
*~
.vscode/
pixi.lock

# Future tree-sitter build artifacts (parser.c is checked in deliberately,
# but build/ and node_modules/ are generated and not tracked)
node_modules/
build/
docs/

# Python bytecode & tool caches
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# pip editable-install build cruft
*.egg-info/
/sublime/test/target/
