=====================
Positional call
=====================

f(x, y)

---

(module
  (call_expression
    (identifier)
    (argument_list (identifier) (identifier))))

=====================
Keyword call
=====================

f(a = 1, b = 2)

---

(module
  (call_expression
    (identifier)
    (argument_list
      (keyword_argument (identifier) (integer))
      (keyword_argument (identifier) (integer)))))

=====================
Mixed call
=====================

f(x, a = 1)

---

(module
  (call_expression
    (identifier)
    (argument_list
      (identifier)
      (keyword_argument (identifier) (integer)))))

=====================
Multiline call
=====================

f(
  x,
  y,
)

---

(module
  (call_expression
    (identifier)
    (argument_list (identifier) (identifier))))

=====================
Field access
=====================

r.field

---

(module
  (field_access (identifier) (identifier)))

=====================
Dot-call broadcast
=====================

v.(x, y)

---

(module
  (dot_call
    (identifier)
    (argument_list (identifier) (identifier))))

=====================
Index simple
=====================

A[i]

---

(module
  (index_expression (identifier) (identifier)))

=====================
Index multi-dim
=====================

A[i, j]

---

(module
  (index_expression (identifier) (identifier) (identifier)))

=====================
Index slice colon
=====================

A[:, j]

---

(module
  (index_expression
    (identifier)
    (slice_selector)
    (identifier)))

=====================
Index all identifier
=====================

A[all, j]

---

(module
  (index_expression
    (identifier)
    (identifier)
    (identifier)))

=====================
Index only identifier
=====================

A[only, j]

---

(module
  (index_expression
    (identifier)
    (identifier)
    (identifier)))

=====================
Index singleton bang
=====================

A[!]

---

(module
  (index_expression
    (identifier)
    (only_selector)))

=====================
Index bang with axes
=====================

v[all, only, :, !]

---

(module
  (index_expression
    (identifier)
    (identifier)
    (identifier)
    (slice_selector)
    (only_selector)))

====================
Mixed args positional then keyword
====================

f(a, b = 2)

---

(module
  (call_expression
    (identifier)
    (argument_list
      (identifier)
      (keyword_argument (identifier) (integer)))))

=====================
Chained postfix
=====================

f(x).field[0]

---

(module
  (index_expression
    (field_access
      (call_expression
        (identifier)
        (argument_list (identifier)))
      (identifier))
    (integer)))

=====================
Module member call
=====================

pp.Landau(loc = 0.0, scale = 1.0)

---

(module
  (call_expression
    (field_access (identifier) (identifier))
    (argument_list
      (keyword_argument (identifier) (float))
      (keyword_argument (identifier) (float)))))

=====================
Axis list as call argument
=====================

C = aggregate(sum, [.i, .k], A[.i, .j] * B[.j, .k])

---

(module
  (binding
    (identifier)
    (call_expression
      (identifier)
      (argument_list
        (identifier)
        (axis_list
          (axis_name (axis_id))
          (axis_name (axis_id)))
        (binary_expression
          (index_expression
            (identifier)
            (axis_name (axis_id))
            (axis_name (axis_id)))
          (index_expression
            (identifier)
            (axis_name (axis_id))
            (axis_name (axis_id))))))))

=====================
Empty axis list as call argument
=====================

aggregate(sum, [], y)

---

(module
  (call_expression
    (identifier)
    (argument_list
      (identifier)
      (axis_list)
      (identifier))))

=====================
Axis list argument with variance markers
=====================

metricsum(g, [.i^, .j_], x)

---

(module
  (call_expression
    (identifier)
    (argument_list
      (identifier)
      (axis_list
        (axis_name (axis_id) (variance_marker))
        (axis_name (axis_id) (variance_marker)))
      (identifier))))
